name: Bump Version - Run
on:
  pull_request:
    types: [closed]
    branches:
      - alpha/v*/v*
      - release/v*/v*
      - main

jobs:
  bump:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Bump Version
        if: ${{ github.event.pull_request.merged }}
        uses: kungfu-trader/action-bump-version@v2
        with:
          bump-keyword: "auto"
          source-ref: ${{ github.head_ref}}
          dest-ref: ${{ github.base_ref }}
      
      - name: Test Failure
        run: |
          exit -1
